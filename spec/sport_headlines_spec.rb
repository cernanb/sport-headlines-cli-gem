require 'spec_helper'

describe SportHeadlines do
  it 'has a version number' do
    expect(SportHeadlines::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
